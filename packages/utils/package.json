{
  "name": "@aproxy/utils",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "rimraf dist && tsc  -p ./tsconfig.utils.json",
    "build": "rimraf dist && tsc  -p ./tsconfig.utils.json",
    "check-types": "tsc --noEmit -p ./tsconfig.utils.json"
  },
  "npmClient": "pnpm",
  "engines": {
    "node": ">=16.13.0"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
