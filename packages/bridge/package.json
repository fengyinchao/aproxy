{
  "name": "@aproxy/bridge",
  "version": "0.0.1",
  "description": "集抓包、调试、mock 于一身的前端开发利器",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "rimraf dist && tsc -p ./tsconfig.bridge.json",
    "build": "rimraf dist && tsc -p ./tsconfig.bridge.json",
    "check-types": "tsc --noEmit -p ./tsconfig.bridge.json"
  },
  "npmClient": "pnpm",
  "engines": {
    "node": ">=16.13.0"
  },
  "dependencies": {
    "@aproxy/utils": "workspace:^0.0.1",
    "rxjs": "7.5.1",
    "ws": "^8.8.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.3"
  }
}
